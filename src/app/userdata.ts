// import * as QRCode from 'qrcode';

// // Generate QR Code with user data
// const userData = { userId: 12345, timestamp: Date.now() };
// const qrCodeText = JSON.stringify(userData);

// QRCode.toDataURL(qrCodeText, (err, url) => {
//   if (err) {
//     console.error(err);
//     return;
//   }
//   // 'url' contains the generated QR code image as a data URL
//   console.log(url);
// });
